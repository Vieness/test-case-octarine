import React from 'react';
import './style.scss'

const Registration = () => {
    return (
        <section className={'section registration-section'}>
            <div className="container">
                <div className="registration">
                    <h1 className="registration-title">Are You New Here ?
                        Don’t wait, Register Now. </h1>
                    <button className="gradient-btn registration-btn">Register Now</button>
                </div>
            </div>
        </section>
    );
};

export default Registration;
